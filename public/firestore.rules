rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // كل الوصول ممنوع من المتصفح. فقط Admin SDK (السيرفر) يتجاوز هذه القواعد.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
